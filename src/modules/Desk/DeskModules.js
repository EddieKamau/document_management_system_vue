import navModules from './nav-modules/NavModule'
import explorerModules from './explorer-modules/ExplorerModule'

const modules = {
    navModules,
    explorerModules
}

export default {
    modules
}